displayName: Full URL Rewrite
type: middleware
import: github.com/e-flux-platform/full-url-rewrite-traefik-plugin

summary: "Rewrite full request URL using regular expressions"

testData:
  regex: https://(\\w+)\\.example\\.com/(.+)
  replacement: https://$1.example.com/$1/$2
